# Mini Project

Slightly larger, application-style Verilog designs — built using the modeling
concepts practiced in the other folders (gate-level, dataflow, behavioral),
combined into a complete working system rather than a single isolated circuit.

## Projects

- **Traffic_Light_Controller** — FSM-based traffic light controller cycling through
  4 states, driving a 6-bit light output pattern
